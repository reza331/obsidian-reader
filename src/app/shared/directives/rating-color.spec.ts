import { RatingColor } from './rating-color';

describe('RatingColor', () => {
  it('should create an instance', () => {
    const directive = new RatingColor();
    expect(directive).toBeTruthy();
  });
});
